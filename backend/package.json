{
  "name": "esms-backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "ts-node src/mqtt-worker.ts",
    "build": "tsc",
    "start": "node dist/mqtt-worker.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate deploy"
  },
  "dependencies": {
    "@prisma/client": "^5.10.2",
    "mqtt": "^4.3.8"
  },
  "devDependencies": {
    "prisma": "^5.10.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.2",
    "@types/node": "^22.5.4"
  }
}
